Unit test - simplest test a little function here and there
Red green refractor
Integrator testing - communication
E2e - the whole behaviour loop, both backend and frontend

Arrange act assert

var regexItem = Regex.Replace(input, "[^0-9A-Za-z_-]", "");

    return regexItem.Replace(" ", "");